<h1>Bem-vindo ao Captive Portal</h1>
<% if user_signed_in? %>
  <p>Você está logado como: <%= current_user.email %></p>
  <%= link_to "Logout", destroy_user_session_path, method: :delete %>
<% else %>
  <p>Você não está logado.</p>
  <%= link_to "Login", new_user_session_path %>
<% end %>
